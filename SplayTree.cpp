#include "SplayTree.h"

using namespace std;

void SplayTree: Insert(string key, string value)
{
  if(root == null)
  {
    root = new TreeNode();
    root->key = key;
    root->value = value;
    root->right = null;
    root->left = null;
  }
  else 
  {
    
  }
}
